#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){
    int line, column;
    char command[10];
    scanf("%d %d \n", &line, &column);
    for(int i = 0; i < line; i++){
        for(int j = 0;j < column; i++){
            
        }
    }
    while(command != 'F'){
        scanf("%s", &command);

    }
    return 0;
}